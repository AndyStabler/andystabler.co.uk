---
title: "Andy Stabler"
pageable: true
---
<% page_articles.each do |article| %>
  <section class="article-snippet">
    <h1 class="article-snippet-header">
      <%= link_to article.title, article.url %>
    </h1>
    <p class="article-snippet-date"><%= article.date.strftime("%d %b %Y") %></p>
    <div>
      <%= Nokogiri::HTML(article.summary).text %>
      <p><%= link_to "Continue Reading &rarr;", article %></p>
    </div>
  </section>
<% end %>
<div class="pagination">
  <%= link_to "Older posts", next_page, :class => "older" if next_page.present? %>
  <%= link_to "Newer posts", prev_page, :class => "newer" if prev_page.present? %>
</div>
<section class="links">
  <a href="/feed.xml"><%= image_tag "rss.png", width: "20px", height: "20px" %> RSS feed (Atom)</a>
  <%= partial "partials/social_links" %>
</section>
